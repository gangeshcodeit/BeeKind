# =============================================================================
# BeeKind — dependency manifest (npm packages used by this app only)
# =============================================================================
# This project is Node.js / npm, not Python. Install with:
#   npm run install:all
# from the repository root (or npm install in client/ and server/ separately).
#
# Pin exact versions for reproducible builds: use package-lock.json in each
# folder after npm install, or run `npm shrinkwrap` if you need a single lock.
# =============================================================================

# --- Runtime platform ---
# Node.js >= 18.x recommended

# --- Root (repo) — dev only, for `npm run dev` ---
concurrently@^8.2.2

# --- server/ (API — production dependencies) ---
bcryptjs@^2.4.3
cors@^2.8.5
dotenv@^16.4.5
express@^4.21.0
jsonwebtoken@^9.0.2
mongoose@^8.7.0
multer@^2.1.1
openai@^4.67.3

# --- client/ (SPA — bundled into production build) ---
@dotlottie/react-player@^1.6.19
@react-three/drei@^9.114.0
@react-three/fiber@^8.17.14
@react-three/postprocessing@^2.16.3
@splinetool/react-spline@^4.1.0
framer-motion@^12.38.0
react@^18.3.1
react-dom@^18.3.1
react-router-dom@^6.28.0
three@^0.169.0

# --- client/ (devDependencies — required for `npm run dev` / `npm run build`) ---
@vitejs/plugin-react@^4.3.3
autoprefixer@^10.4.20
postcss@^8.4.47
tailwindcss@^3.4.14
vite@^5.4.10

# --- workspace self-reference (not published; do not install from registry) ---
# beekind@file:..   (listed in client and server package.json; points at repo root)
